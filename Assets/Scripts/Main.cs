using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Assets.Scripts.lib;
using System.IO;

public class Main : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //UnityHelper instance = UnityHelper.GetInstance();
        //string writepath = Path.Combine(Application.persistentDataPath, "userdata");
        //instance.CreateFile(writepath, "mydata.txt", "this is a test");

        //ArrayList list = instance.ReadFile(writepath);
        //for (int i = 0; i < list.Count; i++)
        //{
        //    Debug.Log("data is :" + list[i]);
        //}
        
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
